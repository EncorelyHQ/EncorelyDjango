"""
Users — URL Configuration
==========================
Endpoints de autenticación y gestión de usuarios.
"""

from django.urls import path

app_name = 'users'

urlpatterns = [
    # Se agregarán en Commit 4: RegisterView, Login JWT, MeView
]
